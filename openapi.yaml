openapi: "3.0.3"

info:
  title: "Sample API"
  version: "1.0.0"

paths:
  "/message":
    get:
      summary: "Sample Api get operation."
      description: "Sample Api get operation."
      responses:
        "200":
          description: "Success operation."
          content:
            application/json:
              schema:
                type: string
                example: "Hello World !"
